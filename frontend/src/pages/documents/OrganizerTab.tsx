import { ComingSoon } from '../../components/ComingSoon';

export function OrganizerTab() {
  return (
    <ComingSoon
      title="File organiser"
      description="Tag, folder, and filter your documents. Backend endpoints at /api/v1/documents and /api/v1/documents/tags are live; the table UI is being built."
    />
  );
}
