import { ComingSoon } from '../../components/ComingSoon';

export function BulkRenameTab() {
  return (
    <ComingSoon
      title="Bulk rename"
      description="Apply prefix/suffix and find-replace across many documents at once. POST /api/v1/documents/bulk-rename is live; the form UI is being built."
    />
  );
}
