import { ComingSoon } from '../../components/ComingSoon';

export function SharingTab() {
  return (
    <ComingSoon
      title="Sharing & permissions"
      description="Per-document view/comment/edit grants. /api/v1/documents/shares is live."
    />
  );
}
