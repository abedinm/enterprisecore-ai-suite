-r requirements.txt
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
# pytest-timeout: forces a per-test wall-clock limit so a hung test (e.g. an
# infinite SSE stream that deadlocks the sync TestClient on Windows) fails
# loudly instead of stalling the whole suite. Run with --timeout=45.
pytest-timeout>=2.3
ruff>=0.8
