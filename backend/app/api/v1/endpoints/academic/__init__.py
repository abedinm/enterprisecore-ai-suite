"""Academic Module Pack endpoint package — exposed under /api/v1/academic.

The parent router lives in ``academic.router`` (see ``router.py`` next to this
file). The package itself is otherwise empty; sub-routers are mounted in
``router.py`` so the file you grep for is the same file that maps URLs.
"""
